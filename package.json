{
  "name": "zenith",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.0.0",
  "workspaces": ["tui", "server"],
  "scripts": {
    "dev:backend": "npm run start --workspace server",
    "dev:frontend": "npm run dev --workspace tui",
    "run": "npm run dev:backend & npm run dev:frontend",
    "lint": "npm run lint --workspace tui && npm run lint --workspace server",
    "build": "npm run build --workspace tui",
    "test": "npm run test --workspace tui && npm run test --workspace server"
  }
}
